
---
layout: null
---
{
    {% comment %} 
        s/author/domain, s/tag/language
    {% endcomment %}
    "articles": [
    {% for post in site.posts limit:100 %}
        {
            "id": "{{ post.url | absolute_url | sha1 }}",
            "title": {{ post.title | jsonify }},
            "excerpt": {{ post.excerpt | jsonify }},
            "url": "{{ post.url | absolute_url }}",
            {% if post.image.size > 1 %}"image": {{ post.image | jsonify }},{% endif %}
            {% if post.link.size > 1 %}"external_url": "{{ post.link }}",{% endif %}
            {% if post.tags.size > 1 %}"tags": {{ post.tags | jsonify }},{% endif %}
            "date": "{{ post.date }}",
            "domain": "{{ site.author }}",
            "translations": {
            {% for tag in post.tags %}
                "{{ tag }}": "{{ tag | append: '/' | append: post.url | absolute_url }}",
            {% endfor %}
            }
        }{% if forloop.last == false %},{% endif %}
    {% endfor %}
    ]
}
